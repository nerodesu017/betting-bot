package discord_commands_errors

var (
	INSUFFICIENT_FUNDS       = "Your balance is too low for that action"
	INSUFFICIENT_PERMISSIONS = "You don't have permission for that"
	TOO_MANY_REQUESTS        = "You've sent too many requests, you need to slow down"
)
